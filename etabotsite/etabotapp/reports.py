"""Designs the output of generating reports to be sent via email"""
import logging


def generate_formatted_report(raw_status_report):
    logging.debug("GENERATING FORMATTED REPORT")
    return raw_status_report
